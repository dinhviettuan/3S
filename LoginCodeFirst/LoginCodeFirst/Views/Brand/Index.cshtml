@model List<LoginCodeFirst.ViewModels.BrandViewModel>
@{
    ViewBag.Title = BrandLocalizer.GetLocalizedHtmlString("lbl_ListBrand");
}


<div class="container-fluid">
    <div class="block-header">
    </div>
    <!-- Basic Examples -->
    <div class="row clearfix">
        <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
            <div class="card">
                <div class="header">
                    <h2> 
                        <a type="button" class="btn btn-primary" data-toggle="ajax-modal" data-target="#add-contact" 

                           asp-controller="Brand" asp-action="Add")>@CommonLocalizer.GetLocalizedHtmlString("btn_Add")</a>
                    </h2>  
                </div>
                <div class="body">
                    <div class="table-responsive">
                        <table class="table table-bordered table-striped table-hover js-basic-example dataTable">
                            <thead>
                            <tr>
                                <th>
                                    @CommonLocalizer.GetLocalizedHtmlString("lbl_NumbericalOrder")
                                </th>
                                <th>
                                    @BrandLocalizer.GetLocalizedHtmlString("lbl_BrandName")
                                </th>
                                <th>
                                    @CommonLocalizer.GetLocalizedHtmlString("lbl_Actions")
                                </th>
                            </tr>
                            </thead>
                            <tbody>
                            @{ var stt = 0;}
                            @foreach (var item in Model)
                            {
                                stt += 1;
                                <tr>
                                    <td>@stt</td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.BrandName)
                                    </td>
                                    <td>

                                        <a asp-controller="Brand" asp-action="Edit" 
                                           asp-route-id="@item.Id">@CommonLocalizer.GetLocalizedHtmlString("btn_Edit")</a>
                                        <a asp-controller="Brand" asp-action="Delete" 
                                           onclick=" return confirm('@CommonLocalizer.GetLocalizedHtmlString("btn_YouDelete")')" 
                                           asp-route-id="@item.Id">@CommonLocalizer.GetLocalizedHtmlString("btn_Delete")</a>
                                    </td>
                                </tr>
                            }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>