@using LoginCodeFirst.Resources
@model List<LoginCodeFirst.ViewModels.User.IndexViewModel>
@inject ResourcesServices Localizer
@{
    ViewBag.Title = "Index User";
}


<div class="container-fluid">
    <div class="block-header">
        <h2>
            @ViewBag.Title  
        </h2>
    </div>
    <!-- Basic Examples -->
    <div class="row clearfix">
        <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
            <div class="card">
                <div class="header">
                    <h2>
                        <a class="btn btn-success" asp-controller="User" asp-action="Add">@Localizer.GetLocalizedHtmlString("ADD")</a>
                    </h2>
                </div>
                <div class="body">
                    <div class="table-responsive">
                        <table class="table table-bordered table-striped table-hover js-basic-example dataTable">
                            <thead>
                            @if (TempData["User"] != null)
                            {
                                <div class="alert alert-success alert-dismissible">
                                    <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
                                    <strong>@TempData["User"]</strong>
                                </div>
                            }
                            <tr>
                                <th>
                                    @Localizer.GetLocalizedHtmlString("ID")
                                </th>
                                <th>
                                    @Localizer.GetLocalizedHtmlString("StoreId")
                                </th>
                                <th>
                                    @Localizer.GetLocalizedHtmlString("Email")
                                </th>
                                <th>
                                    @Localizer.GetLocalizedHtmlString("FullName")
                                </th>
                                <th>
                                    @Localizer.GetLocalizedHtmlString("Phone")
                                </th>
                                <th>
                                    @Localizer.GetLocalizedHtmlString("IsActiver")
                                </th>
                                <th>
                                    @Localizer.GetLocalizedHtmlString("Actions")
                                </th>
                            </tr>
                            </thead>
                            <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.UserId)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Store.StoreName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Email)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.FullName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Phone)
                                    </td>                                    
                                    <td>
                                        @Html.CheckBoxFor(modelItem => item.IsActive)
                                    </td>
                                    <td>
                                        <a asp-controller="User" asp-action="EditPassword" asp-route-id="@item.UserId">@Localizer.GetLocalizedHtmlString("Password")</a>
                                        <a asp-controller="User" asp-action="Edit" asp-route-id="@item.UserId">@Localizer.GetLocalizedHtmlString("Edit")</a> |
                                        <a asp-controller="User" asp-action="Delete" onclick=" return confirm('You really want to delete?')" asp-route-id="@item.UserId">@Localizer.GetLocalizedHtmlString("Delete")</a>
                                    </td>
                                </tr>
                            }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>